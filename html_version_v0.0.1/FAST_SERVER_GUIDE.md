# ⚡ 고속 서버 가이드

## 빠른 시작

```bash
start_server_fast.bat
```

## 성능 비교

| 서버 | 요청/초 | 응답시간 | 동시접속 |
|-----|--------|---------|---------|
| Flask 개발 서버 | ~100 | 50-200ms | 제한적 |
| **Waitress** | **1000-2000** | **10-50ms** | **100+** |

## 주요 개선사항

### 1. ⚡ Waitress WSGI 서버
- **Windows 최적화** 프로덕션 WSGI 서버
- 멀티스레딩 (4 threads)
- 비동기 I/O
- 자동 연결 풀링
- **10-20배 빠른 처리 속도**

### 2. 🗄️ 확장된 캐싱
- OHLCV 캐시 TTL: **180초 (3분)**
- 캐시 용량: **300개 항목**
- 자동 메모리 관리

### 3. 🔧 자동 최적화
- HTTP 연결 재사용
- gzip 압축 (70% 전송량 감소)
- 불필요한 로그 제거
- 빠른 실패 (Fast Fail)

## 설치된 패키지

```txt
waitress==3.0.0          # 프로덕션 WSGI 서버
Flask-Compress==1.14      # gzip 압축
Flask-Caching==2.1.0      # 자동 캐싱
```

## 추가 최적화 (선택사항)

### Redis 캐싱 (더 빠름)
```bash
pip install redis flask-caching[redis]
```

### 멀티프로세스 모드
app.py 수정:
```python
serve(app, host='0.0.0.0', port=5000, threads=8)  # 8 threads
```

## 모니터링

서버 상태 확인:
```
http://localhost:5000/api/health
http://localhost:5000/api/cache/stats
```

## 문제 해결

### Waitress 설치 오류
```bash
pip install --upgrade pip
pip install waitress==3.0.0
```

### 포트 충돌
app.py의 포트 변경:
```python
serve(app, host='0.0.0.0', port=5001, threads=4)
```

---
**참고:** 개발 중 디버깅이 필요하면 기존 `start_server.bat` 사용
